requests>=2.31
pandas>=2.0
numpy>=1.24
python-dotenv>=1.0
tabulate>=0.9

# Optional: SinoPac Shioaji intraday realtime quotes (works without it; falls back to free TWSE MIS)
# Install manually if needed:  pip install shioaji
# shioaji>=1.2
