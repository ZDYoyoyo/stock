# 指令手冊（所有功能怎麼跑）

> 在 `stock` 資料夾裡執行；Windows 先跑一次 `chcp 65001`。
> 前置：先有資料 → `python -m scripts.update_data`（第一次加 `--days 40`）。

---

## 一、每天必跑

| 目的 | 指令 |
|---|---|
| 🌅 盤前快報（開盤前看，含昨夜美股/費半） | `python -m scripts.run_morning` |
| 更新資料（盤後） | `python -m scripts.update_data` |
| 資料存回CSV累積（雲端用） | `python -m scripts.sync_data dump` ／ 重建：`python -m scripts.sync_data load` |
| 一鍵跑全部+環境（產出 .md 和 .html） | `python -m scripts.run_all` |
| 只跑波段（快，不含長期） | `python -m scripts.run_all --skip-longterm` |
| 更快（連 T11 排雷也略過） | `python -m scripts.run_all --skip-longterm --skip-landmine` |
| **跑完推播摘要到手機** | `python -m scripts.run_all --notify` |

> `run_all` 現在含：🚦環境 + 波段(T11/T16) + **🚀成長T12** + 長期 + 當沖 + 持股，
> 且 **T11 清單會就地排雷、標「風險」欄**（選股當下就看到雷；有🔴/🟠會列紅旗提醒）。
> 排雷需 FinMind 逐檔查 T11 候選（清單小、成本低）；想更快可加 `--skip-landmine`。

### FinMind token 設定（選填；深掘/回測用免費 token，分點用付費 Sponsor）

FinMind 分兩種，都填在 `.env` 的同一個 `FINMIND_TOKEN`：

**A. 免費 token（建議先辦，提高額度，深掘/回測較不會卡限流）**
1. 開 https://finmindtrade.com/ → 右上「註冊/登入」（用 Email 或 Google）
2. 登入後進「會員中心 / API Token」頁 → 複製你的 **API Token**（一長串）
3. 在 stock 資料夾：`copy .env.example .env` → 用記事本開 `.env`
4. 填這行後存檔（存檔類型選「所有檔案」，別存成 .env.txt）：
   ```
   FINMIND_TOKEN=你複製的那串token
   ```
5. 免費 token 就生效了（run_t30 / run_landmine / 回測會用它，額度較高）。

**B. 付費 Sponsor（要看「券商分點主力進出」才需要）**
1. 同一個 FinMind 帳號 → https://finmindtrade.com/analysis/#/Sponsor/sponsor 訂閱贊助方案
2. **token 不變**（還是同一串），升級後那串 token 的權限自動涵蓋分點
3. 什麼都不用改，直接跑 `python -m scripts.run_mainforce` → 開頭會顯示
   `分點資料：✅ 可用（FinMind Sponsor）`，並多出「分點主力買賣超」欄

**驗證有沒有生效**：
```bat
python -m scripts.run_mainforce
```
開頭那行：`⚪ 免費模式` = 沒 token 或免費等級；`✅ 可用（FinMind Sponsor）` = 分點已解鎖。

> ⚠️ token 等同密碼，別貼到公開場合、別 commit 進 git（`.env` 已被 .gitignore 排除）。

### 推播設定（Telegram，取代已停用的 Line Notify）
1. 手機 Telegram 搜 `@BotFather` → 傳 `/newbot` → 取得 **BOT TOKEN**
2. 對你的新 bot 傳一則訊息，再瀏覽器開
   `https://api.telegram.org/bot<你的TOKEN>/getUpdates` → 找 `"chat":{"id":數字}` = **CHAT_ID**
3. 把 TOKEN、CHAT_ID 填進 `.env`（參考 `.env.example`）
4. 跑 `python -m scripts.run_all --notify` → 手機就會收到摘要 + 完整 HTML 檔
（也支援 Email，填 `.env` 的 SMTP_* 即可；Telegram 優先。）

報告在 `reports\screener\`，**用瀏覽器開 `_run_all.html`** 最清楚。

---

## 二、各軌單獨跑

| 軌道 | 指令 | 說明 |
|---|---|---|
| 波段掃描 | `python -m scripts.daily_scan` | 環境+T11+T16+交集 |
| 🚀 月營收動能 T12 | `python -m scripts.run_t12` | 營收YoY強+近月加速的成長股（免FinMind，每月10號後更新） |
| 🏦 主力籌碼綜合 | `python -m scripts.run_mainforce` | 主力進駐排行（法人+千張+融資）；`--stocks 2330` 看單檔 |
| 長期價值 | `python -m scripts.run_longterm` | 價值+成長+配息（較慢） |

> 🏦 **分點是 FinMind 付費(Sponsor)**。免費時 `run_mainforce` 走「法人+千張+融資」綜合；
> 之後辦 Sponsor、把 token 填進 `.env` 的 `FINMIND_TOKEN`，會**自動加出分點主力買賣超欄**，不需改程式。
| 當沖候選 | `python -m scripts.run_daytrade` | 高波動+高流動 |

---

## 三、深掘（研究個股用）

### 🔍 個股籌碼診斷（外資/投信/自營 + 融資融券 + 今日漲跌原因）

```
python -m scripts.diagnose 2330            # 診斷台積電
python -m scripts.diagnose 4939 --days 15  # 看更長天數
```
輸出：三大法人**逐日拆細**（外資/投信/自營/合計）＋融資餘額/增減＋融券餘額/增減＋量能，
並用**今日 vs 昨日**的籌碼變化做「漲跌歸因」（例：法人買/散戶追/軋空/散戶接刀…）。
GUI 對應按鈕：**🔍 籌碼診斷**（填代號後按）。


| 目的 | 指令 | 說明 |
|---|---|---|
| **基本面深掘** | `python -m scripts.run_t30` | 對 T11 短名單補月營收/EPS/PER**＋千張大戶籌碼** |
| **千張大戶更新** | `python -m scripts.update_holders` | 抓集保股權分散（**每週跑一次**，累積趨勢） |
| **風控試算** | `python -m scripts.risk_calc --stock 2206 --account 1000000 --risk 1.5` | 算停損/停利/張數 |

### 🧨 地雷偵測（買進/續抱前排雷，是「排除」非選股）
| 目的 | 指令 |
|---|---|
| **查我的持股**（預設） | `python -m scripts.run_landmine --holdings` |
| **查指定股**（要買前先掃） | `python -m scripts.run_landmine --stocks 2330,2603` |
| **掃今日候選**（避免買到雷） | `python -m scripts.run_landmine --from-daily` |
| 只列風險≥某級 | `python -m scripts.run_landmine --holdings --min-level 高` |

查財務(營收/EPS/毛利)+籌碼(法人賣/大戶出貨/融資暴增)+技術(破季線/相對弱勢)紅旗，
加權給風險級 🟢低/🟡中/🟠高/🔴嚴重。門檻在 `src/config.py` 的 `LANDMINE`。

### 持股追蹤器（記錄實單、算損益、停損警示）
| 目的 | 指令 |
|---|---|
| 看持股與即時損益 | `python -m scripts.portfolio` |
| 新增/更新一筆 | `python -m scripts.portfolio add 2330 --lots 2 --price 1000 --stop 950 --target 1100` |
| 移除 | `python -m scripts.portfolio remove 2330` |

持股存在 `data\portfolio.csv`（可用 Excel 編輯，不會上傳）。到停損/停利價會跳 🔔 警示。
盤中即時價用 MIS、盤後用收盤；損益為毛額（未計手續費+證交稅約0.6%）。

| **盤中持股守衛**（掛著跑，觸停損自動推 Telegram） | `python -m scripts.monitor_portfolio` |
| 守衛-調整檢查頻率 | `python -m scripts.monitor_portfolio --interval 30` |

盤中(09:00~13:30)在本機掛著跑，每檔觸停損/停利只推一次。需設 Telegram 才會推手機。

> 千張大戶「週增減」要**連續跑幾週**才會有數字（第一次只有當週水位）。

---

## 四、回測（驗證訊號有沒有效）

| 種類 | 指令 | 時間 |
|---|---|---|
| 單/少檔快測 | `python -m scripts.run_backtest --stocks 2206,6669 --hold 10 --k 4` | 快 |
| 一籃子驗證 | `python -m scripts.run_validation --hold 10 --k 4` | 中 |
| 訊號 vs 大盤基準（做多） | `python -m scripts.run_edge_check` | 中 |
| **完整回測（做多）** | `python -m scripts.run_full_backtest --universe 60 --start 2022-01-01` | **20~40分（會限流等待，正常）** |
| 🩳 **地雷放空回測** | `python -m scripts.run_short_backtest --universe 80 --start 2022-01-01 --rev-bad -20` | **20~40分** |
| 🎯 **觸發器甜蜜點回測** | `python -m scripts.run_trigger_backtest --universe 80 --start 2022-01-01` | **20~40分** |

> 🎯 **觸發器回測**：地雷進觀察清單後，比較「即空/破季線/破月線/破近低/動能翻負」哪個放空點最好。
> 結論(2026-07)：**等技術觸發器反而更差**，關鍵是「地雷當下已走弱就即刻空、價強別碰」。
> 報告 → `reports/trigger_backtest_validation.md`。

> 🩳 **地雷放空回測**：驗證「財務地雷股放空」有沒有 edge，並分「地雷+跌破季線(弱)」
> vs「地雷+站上季線(強)」測軋空風險。含放空成本(手續費+證交稅+借券)。
> 報告 → `reports/short_backtest_validation.md`。⚠️ 放空風險遠高於做多，測歸測、實戰務必嚴設回補停損。

參數：`--hold` 持有天數、`--k` 法人連買門檻天數、`--start` 起始日、`--stocks` 指定股。
完整回測結論已存 `reports\backtest_validation.md`。

---

## 五、盤中監控（盤中 09:00~13:30 才有即時）

| 目的 | 指令 |
|---|---|
| 盯指定股 | `python -m scripts.monitor_intraday --stocks 2330,2454` |
| 盯當沖候選前10 | `python -m scripts.monitor_intraday --from-daytrade 10` |
| 只抓一次(測試) | `python -m scripts.monitor_intraday --stocks 2330 --once` |

即時報價預設用**免費 TWSE MIS**（免開戶、約 20 秒延遲）。若設定了永豐金 Shioaji（下方），會**自動改用 Shioaji**（更快更準），不需改指令。

### 永豐金 Shioaji 即時行情設定（選填；盤中即時升級）

**定位**：把盤中監控的報價從「20 秒延遲」升級成「券商級即時」。本系統**只用行情、不下單**。

**申請要開什麼權限**（重點）：
- ✅ **開「正式環境 ＋ 行情」** —— 行情屬「非個人資料」，只需 API Key 登入驗證。
- ❌ **不用開「下單」** —— 下單才需要電子憑證＋臨櫃簽「API 電子交易風險預告書」＋模擬測試審核；純看行情完全不需要。

**步驟**：
1. 需有永豐金證券帳戶（可線上開戶）。
2. 到永豐金「API 服務」網頁申請 API，取得 **API Key** 與 **Secret Key**。
3. `pip install shioaji`
4. 把金鑰填進 `.env`（本機）或環境變數（雲端）：
   ```
   SHIOAJI_API_KEY=你的_api_key
   SHIOAJI_SECRET_KEY=你的_secret_key
   ```
5. 核對：`python -m src.shioaji_client` → 印出 2330/2317/6243 即時報價就 OK。

**限制**（永豐金官方）：訂閱上限 200 檔、每身分證最多 5 連線、每 24h 需重新登入；行情流量額度**依下單量而定** → 適合盯**小清單**（持股＋當沖候選），別大量抓歷史。沒設定或抓不到時系統自動退回免費 MIS。

---

## 六、常見問題

- **一定要先 `update_data`** 才能跑選股/回測（靠 DB）。
- **回測/深掘很慢**是正常的（FinMind 免費限流會自動等待，看到 `402 等60s` 別緊張）。
- **表格看起來歪** → 用瀏覽器開 `.html`，或 VSCode 按 `Ctrl+Shift+V` 預覽 `.md`。
- **改門檻**：全部在 `src/config.py`（殖利率、連買天數、振幅…）。
- 功能白話解釋見 `docs/功能說明.md`。
